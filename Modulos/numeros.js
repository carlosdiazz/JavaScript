const numeros = [1,2,3,4]

//Por defecto me va a exportar numeros
export default numeros

export const sumar = (a,b) => a + b

export const restar = (a,b) => a - b

export const dividir = (a,b) => a / b